// Sum Numbers
function sum (numbers) {
    "use strict";
    let sum = 0; // initial sum is zero
    numbers.forEach(number => sum += number); // takes the sum of all numbers
    return sum;
      
      
  };